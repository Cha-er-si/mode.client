import { AutoScroll } from './auto-scroll';

describe('AutoScroll', () => {
  it('should create an instance', () => {
    const directive = new AutoScroll();
    expect(directive).toBeTruthy();
  });
});
